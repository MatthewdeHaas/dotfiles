# Dotfiles

My personal config files to keep environments consistent.


## Installation 
```bash
mkdir ~/.dotfiles && cd .dotfiles
git clone git@github.com:MatthewdeHaas/dotfiles.git .
bash install.sh
```
